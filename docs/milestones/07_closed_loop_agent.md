# Milestone 07 — Closed-loop agent

## Goal
Run the full read -> plan -> validate -> execute -> diff -> update loop.

## Outcomes
- complete agent loop runs
- memory updates happen after each step
- progress and stuck logic are active
- logs are useful enough to debug agent decisions

## Definition of done
- mock mode can run multiple turns end-to-end
- PyBoy mode can run once state/control are stable enough
